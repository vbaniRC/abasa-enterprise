export async function requireAuth(req: any, NextResponse: any) {
  // Placeholder auth check
  // U pravoj verziji bi ovdje provjeravao token, session itd.
  // Za sada samo vraća true da build prođe.
  return true;
}
